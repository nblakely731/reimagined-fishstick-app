1. Get the application URL by running these commands:

{{- if .Values.ingress.enabled }}
  Ingress enabled:
  
  export HOST={{ (index .Values.ingress.hosts 0).host }}
  echo "http://$HOST/"

{{- else }}
  Service (ClusterIP) exposed on port {{ .Values.service.port }}:

  kubectl port-forward svc/{{ include "liatrio-demo-app.fullname" . }} 8080:{{ .Values.service.port }}
  echo "http://localhost:8080/"
{{- end }}